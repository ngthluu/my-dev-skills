# Brainstorm decision graph validation — 2026-09-25

The contract is [the decision graph spec](specs/2026-09-25-brainstorm-decision-graph.md). The starting ref was `eaca990c0e5d6c609abf1f2c4d5f91d678e50b11`. The explicit-invocation metadata and README compatibility note were already in the working tree before implementation.

`tests/brainstorm-instructions.test.mjs` checks that the packaged instructions contain the graph, ledger, reconciliation, single-question, tool fallback, and no-confirmation rules without the old conflicting directions. `tests/metadata.test.mjs` parses all three skills and Codex sidecars for explicit-invocation settings. These are static guardrails, not proof that a host or model follows the instructions.

A read-only Codex CLI v0.157.0 run loaded the repository's `skills/brainstorm/SKILL.md` for a simulated activity-export interview. It inspected the workspace, displayed an initial dependency ledger, and asked a single eligible audience/purpose decision with a recommendation and tradeoff. An earlier run repeated its question in commentary and final output. The instruction was tightened to prohibit restating the question; the repeat run displayed one question, although it put that question in commentary rather than the requested final response. A separate transcript-continuation run supplied one user reply settling audience, format, and delivery together. Codex marked all three settled, recorded that reply as their source, and asked a new scope question rather than repeating any of them. No files were written. These runs cover chat fallback and one reconciliation turn; they did not load an installed copy or exercise a host question UI.

A separate read-only scenario simulation walked partial and complete answers, later contradiction, a newly revealed prerequisite, ordering, tool-present and tool-absent choices, and completion. It found the corresponding instructions but was not an application runtime replay. Longer multi-turn behavior, spec generation, host question-tool behavior, and manual invocation across Claude Code, Cursor, and OpenCode V2 remain unverified. Metadata parsing alone does not establish application loading behavior.
