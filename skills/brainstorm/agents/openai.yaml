interface:
  display_name: "Brainstorm"
  short_description: "Turn deep discovery into an implementation spec"
  default_prompt: "Use $brainstorm to question this idea until it is ready for implementation."
